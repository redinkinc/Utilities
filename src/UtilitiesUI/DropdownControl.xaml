<UserControl x:Class="Utilities.DropdownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="210">
    <StackPanel Name="CustomUIGrid">
        <ComboBox Name="Box" ItemsSource="{Binding}" SelectionChanged="ComboBox_SelectionChanged" Width="Auto" SelectedItem="{}"/>
        <WrapPanel Margin="5" HorizontalAlignment="Center">
            <Button Name="btnPrevious" Click="btnPrevious_Click" Width="95" HorizontalAlignment="Left">Previous</Button>
            <Button Name="btnNext" Click="btnNext_Click" Margin="5,0" Width="95" HorizontalAlignment="Right">Next</Button>
        </WrapPanel>
    </StackPanel>
</UserControl>
